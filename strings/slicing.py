name = 'omgajera12345'
print(name[0:-2])
 print(name[1:12:2]) # skipns 1 character(n-1)

 print(name[:10])    # replace the first empty number with 0;
 print(name[1:])    # repalce the second empty number with length - 1;
